#!/bin/bash
# Telamon-generated shim. The real implementation is the forge-generic
# `issues` CLI from the tulisalama-tools package (installed in every dev
# container and dev box); it picks gh or the Gitea API from the origin
# remote and resolves tokens from env or the project sops store.
exec issues create-enhancement "$@"